#!/bin/bash

pipeline {
    agent any

    environment {
         branch = '$selectBranch'
         deploy = '$deployEnv'
    }
    stages{
        stage('Enviroment'){
            steps{
                load "deploy/jenkins/\"$deploy\"/jenkinsfile"
            }
        }
    }
}
