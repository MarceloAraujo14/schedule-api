#!/bin/bash

pipeline {
    agent any

    environment {
         branch = '$selectBranch'
         deploy = '$deployEnv'
    }
    stages{
        stage('Enviroment'){
            steps{
                sh "echo ./deploy/jenkins/\"$deploy\"/jenkinsfile"
            }
        }
    }
}
