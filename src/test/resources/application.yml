#TEST PROPERTIES
server:
  port: 8082

spring:
  profiles:
    - test
#Database TEST Conf
  datasource:
    url: jdbc:h2:mem:testdb

  h2:
    console:
      enabled: true

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    locations: "db/migration"
    baseline-on-migrate: false